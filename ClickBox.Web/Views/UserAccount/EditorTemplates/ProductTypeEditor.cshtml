@using ClickBox.Web.Infrastructure
@using ClickBox.Web.Models;
@model string


@(
    Html.Kendo().DropDownListFor(m => m).DataTextField("Text").DataValueField("Value")
    .Name("Product")
    .Value(Model)
    .SelectedIndex(0)
    .BindTo(Extensions.GetProductList())
)
